{{ form_start(form, {attr: {id: 'user-form'}}) }}
    {{ form_widget(form) }}
{{ form_end(form) }}

<div class="form-group row">
    <div class="col-lg-4 col-lg-offset-2">
        <a class="btn btn-white btn-sm" href="{{ path('app_admin_user_index') }}">back to list</a>
        <button class="btn btn-primary btn-sm" form="user-form" type="submit">{{ button_label|default('Save') }}</button>

        {% if user.id > 0 %}
            {{ include('admin/user/_delete_form.html.twig') }}
        {% endif %}
    </div>
</div>

